

export default function handler(req, res) {
  //update status  into the order table after chicking and confirming payment the transaction
  //Initiate Shiping
  //Redirect user the order confirmation page
    res.status(200).json({body:req.body })
  }
  
  